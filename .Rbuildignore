^.*\.Rproj$
^\.Rproj\.user$

^\.git$
^\.github$


^codecov\.yml$
^_pkgdown\.yml$
checkout\.txt$
lintoutput\.txt$
^LICENSE\.md$

^CRAN-SUBMISSION$
^cran-comments\.md$
^CODE_OF_CONDUCT\.md$

^CLAUDE\.md$
^\.claude$

# Measurements of API-side pagination and latency problems, written to be filed
# against the API repo. Not package documentation.
^NEOTOMA-API-ISSUES\.md$

# Hand-run script that prepares the cached objects the workshop vignettes read.
^data-raw$

^doc$
^Meta$
^.*\.tar\.gz$

^codemeta\.json$
^CITATION\.cff$
paper.*

^\.Rbuildignore$

# Recorded httptest API responses: used only by the mocked (offline) tests,
# which skip_on_cran(). Kept out of the CRAN tarball to hold its size down. The
# hand-made parser fixtures at fixtures/*.json are NOT ignored — the offline
# parser tests run on CRAN and need them.
^tests/testthat/fixtures/api\.neotomadb\.org/

## Vignettes
^vignettes/SISAL
^vignettes/complex_workflow
^vignettes/simple_workflow
^vignettes/images/
^vignettes/data/
^data-raw/